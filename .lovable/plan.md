

The user is asking for a status check, not a new implementation. Let me verify the current state of both systems by reading the actual code.

Checking:
1. Rejoin call system bugs (lonely timer, unload handler, cross-device hangup)
2. WASAPI window audio capture
