// SwiftPM CubblyKit module — kept minimal for now. As we extract
// platform-agnostic logic (model parsing, formatting helpers) it'll move here
// so it can be unit-tested via `swift test` from the CLI.

public enum CubblyKit {
    public static let version = "0.1.0"
}
